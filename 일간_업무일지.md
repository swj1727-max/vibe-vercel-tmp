# 일간 업무일지

## 2026-05-27

### 업무 주제

- 바이브코딩을 활용한 CSV 데이터 시각화 대시보드 제작
- 장비 유지보수 CSV를 기반으로 실습 발표용 단일 HTML 대시보드 구현

### 진행 기록

1. 작업 목표 확인
   - CSV 파일들을 시각화하는 실습 흐름을 확인했다.
   - 가장 적합한 CSV를 추천하고, 대시보드 형태로 제작하는 것을 목표로 정했다.

2. 작업공간 탐색
   - 현재 작업 폴더의 CSV, HTML, 계획 문서를 확인했다.
   - `실습자료들` 폴더에서 사용 가능한 CSV 파일 목록을 확인했다.
   - 주요 후보로 장비유지보수, 자재단가, 검사결과, 점검현황 데이터를 비교했다.

3. CSV 적합도 판단
   - `장비유지보수_2022-2024.csv`를 추천 데이터로 선정했다.
   - 선정 이유는 229건의 충분한 행 수, 2022-2024년 기간 데이터, 날짜/장비/위치/이상유형/담당자/수리시간/부품교체/비용 컬럼을 모두 포함한다는 점이다.
   - 발표용 대시보드에서 KPI, 추이, 분포, 고위험 작업 목록을 만들기에 가장 적합하다고 판단했다.

4. 대시보드 구현 계획 수립
   - 실습 발표용 스토리형 대시보드로 방향을 정했다.
   - 화면 흐름은 CSV 추천 이유, 핵심 KPI, 월별 추이, 이상유형 분포, 설치위치별 현황, 고비용/장시간 작업 목록 순서로 구성했다.
   - 서버 없이 브라우저에서 바로 열 수 있는 단일 HTML 산출물을 목표로 정했다.

5. 기존 HTML 점검
   - `장비유지보수_대시보드.html` 파일을 확인했다.
   - 기본 대시보드 구조는 있었지만, 발표용 추천 스토리 섹션이 부족하다는 점을 확인했다.
   - 원본 CSV가 UTF-8로 읽을 경우 한글이 깨질 수 있어 CP949 계열 인코딩 처리가 필요하다는 점을 확인했다.

6. 대시보드 재구성
   - `장비유지보수_대시보드.html`을 단일 HTML 대시보드로 갱신했다.
   - 원본 CSV는 수정하지 않고, 데이터를 UTF-8 HTML 안에 내장했다.
   - 외부 CDN 없이 HTML, CSS, vanilla JavaScript, SVG 기반으로 구현했다.

7. 주요 기능 구현
   - 추천 CSV와 선정 이유를 보여주는 발표용 첫 화면을 추가했다.
   - 총 유지보수 건수, 총 비용, 평균 수리시간, 부품교체 비율 KPI를 구현했다.
   - 월별 유지보수 건수와 비용 추이 차트를 구현했다.
   - 이상유형별 발생 분포와 설치위치별 비용 현황을 막대형 시각화로 구현했다.
   - 고비용/장시간 작업 목록을 표로 구현했다.
   - 연도, 설치위치, 이상유형, 부품교체 여부 필터와 필터 초기화 버튼을 추가했다.

8. 검증
   - 필수 문구와 대시보드 섹션이 HTML에 포함되어 있는지 확인했다.
   - 한글 깨짐 의심 문자가 없는지 확인했다.
   - 내장 데이터가 229건인지 확인했다.
   - 2024년 필터 적용 시 KPI가 73건으로 계산되는지 확인했다.
   - SVG 차트와 작업 목록이 렌더링되는지 확인했다.
   - 외부 의존성 마커가 없는지 확인했다.

### 산출물

- `index.html`
  - 장비 유지보수 CSV 기반 단일 HTML 대시보드
  - 필터형 KPI/차트/표 포함
  - 발표용 CSV 추천 스토리 포함

### 추가 진행 기록

1. 대시보드 파일명 변경
   - GitHub Pages와 일반 웹 배포에서 기본 진입 파일로 쓰기 쉽도록 `장비유지보수_대시보드.html` 파일명을 `index.html`로 변경했다.
   - 문서 내 산출물 파일명도 `index.html` 기준으로 정리했다.

2. GitHub 반영 시도
   - 현재 환경에서 `git --version`과 `git status --short` 명령을 실행해 Git 사용 가능 여부를 확인했다.
   - `git` 명령이 설치되어 있지 않거나 PATH에 잡혀 있지 않아 로컬에서 GitHub 커밋/푸시는 진행하지 못했다.
   - GitHub 반영을 위해서는 Git 설치 또는 Git이 사용 가능한 환경에서 커밋/푸시가 필요하다.

### 성과로 가공 가능한 항목

- CSV 후보군을 비교해 대시보드 적합 데이터를 선정했다.
- 한글 인코딩 이슈를 발견하고 UTF-8 HTML 산출물로 해결했다.
- 서버나 외부 라이브러리 없이 실행 가능한 독립형 대시보드를 제작했다.
- 실습 발표 흐름에 맞춘 데이터 스토리텔링 화면을 구성했다.
- 필터 변경에 따라 KPI, 차트, 표가 함께 갱신되는 인터랙티브 기능을 구현했다.
- 구현 후 데이터 수, 필터 결과, 차트 렌더링, 외부 의존성 여부를 검증했다.

### 다음 작업 후보

- 대시보드 화면을 실제 브라우저에서 열어 시각적으로 최종 점검한다.
- 일간 업무일지를 누적해 주간/월간 성과 요약으로 변환한다.
- 업무일지 데이터를 CSV 또는 JSON으로 구조화해 업적 시각화 대시보드의 입력 데이터로 활용한다.
