# 장비 유지보수 CSV 시각화 대시보드 계획

## Summary

- 시각화 대상은 `실습자료들\Module6_Codex데이터자동화_실습자료_장비유지보수_2022-2024.csv`로 한다.
- 이 파일은 229행, 2022-2024년 날짜 데이터, 비용, 수리시간, 이상유형, 담당자/위치, 부품교체여부가 있어 대시보드용으로 가장 적합하다.
- 결과물은 별도 앱 세팅 없이 열 수 있는 단일 HTML 대시보드로 만든다.

## Key Changes

- `index.html`을 생성한다.
- CSV를 읽어 HTML 안에 JSON 데이터로 내장해 `file://`로 열어도 동작하게 한다.
- 화면 구성:
  - KPI: 총 유지보수 건수, 총 비용, 평균 수리시간, 부품교체 비율
  - 월별 추이: 유지보수 건수와 비용
  - 이상유형별 분포
  - 설치위치별 유지보수 현황
  - 고비용/장시간 작업 목록
- 필터:
  - 연도
  - 설치위치
  - 이상유형
  - 부품교체 여부

## Interface / Data

- 외부 API나 서버는 사용하지 않는다.
- 입력 데이터는 CSV의 기존 컬럼을 그대로 사용한다:
  `날짜`, `장비ID`, `장비명`, `설치위치`, `이상유형`, `담당자`, `수리시간_시간`, `부품교체여부`, `비용_원`, `비고`
- 차트는 외부 CDN 없이 HTML/CSS/vanilla JS 기반 SVG 또는 DOM 차트로 구현한다.

## Test Plan

- HTML 파일을 브라우저에서 직접 열어 화면이 깨지지 않는지 확인한다.
- 전체/연도/설치위치/이상유형/부품교체 필터 조합별로 KPI와 차트 값이 갱신되는지 확인한다.
- 필터 결과가 0건일 때 빈 상태 메시지가 표시되는지 확인한다.
- 한글 파일명, 한글 컬럼, 한글 값이 깨지지 않는지 확인한다.

## Assumptions

- `장비유지보수_2022-2024.csv`도 같은 내용 계열이지만 CP949 인코딩이라 웹 대시보드에는 UTF-8 파일인 `Module6_Codex데이터자동화_실습자료_장비유지보수_2022-2024.csv`를 사용한다.
- 원본 CSV 파일은 수정하지 않는다.
- 실습 목적이므로 복잡한 백엔드나 React 세팅보다 단일 HTML 산출물을 우선한다.
